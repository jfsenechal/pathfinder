{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Modifier{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3>Modificateur pour {{ object.name }}</h3>
        </div>
        <div class="card-body">

            <table class="table table-bordered">
                <tr>
                    <th>Class name</th>
                    <td>
                        {{ modifier.object_class }}
                    </td>
                </tr>
                <tr>
                    <th>Id class</th>
                    <td>
                        {{ modifier.object_id }}
                    </td>
                </tr>
                <tr>
                    <th>Caractéristique</th>
                    <td>
                        {{ modifier.ability.value }}
                    </td>
                </tr>
                <tr>
                    <th>Valeur</th>
                    <td>
                        {{ modifier.value }}
                    </td>
                </tr>
            </table>

        </div>
        <div class="card-footer">

        </div>
    </div>
{% endblock %}
