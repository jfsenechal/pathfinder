{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Armes{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h3>Les {{ weapons|length }} armes</h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered" >
                <tr>
                    <th>Nom</th>
                    <th>Campagne</th>
                    <th>Set</th>
                </tr>
                {% for weapon in weapons %}
                    <tr>
                        <td>
                            <a href="{{ path('pathfinder_race_show', {'id': weapon.id}) }}">
                                {{ weapon.name }}
                            </a>
                        </td>
                        <td>

                        </td>
                        <td>

                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>

{% endblock %}
