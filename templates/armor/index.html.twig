{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Armures{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h3>Les {{ armors|length }} armures</h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered" >
                <tr>
                    <th>Nom</th>
                    <th>Campagne</th>
                    <th>Set</th>
                </tr>
                {% for armor in armors %}
                    <tr>
                        <td>
                            <a href="{{ path('pathfinder_race_show', {'id': armor.id}) }}">
                                {{ armor.name }}
                            </a>
                        </td>
                        <td>

                        </td>
                        <td>

                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>

{% endblock %}
