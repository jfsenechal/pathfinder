{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Spell{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3>{{ characterClass }}</h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <tr>
                    <th>Dé de vie</th>
                    <td>{{ characterClass.dieOfLive }}</td>
                </tr>

            </table>
            <h4>Sorts</h4>
            <table class="table table-bordered">
                <tr>
                    <th>Nom</th>
                    <th>Level</th>
                </tr>
                {% for spellClass in spellsClass %}
                    <tr>
                        <td>
                            <a href="{{ path('pathfinder_spell_show', {'id': spellClass.spell.id}) }}">
                                {{ spellClass.spell.name }}
                            </a>
                        </td>
                        <td>{{ spellClass.level }}</td>
                    </tr>
                {% endfor %}
            </table>

            <br/>
        </div>
    </div>
{% endblock %}
