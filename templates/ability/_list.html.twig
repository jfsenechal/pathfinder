<table class="table table-bordered">
    <tr>
        <th></th>
        <th>Total</th>
        <th>Base</th>
        <th>Caractéristique</th>
    </tr>
    {% for ability in abilities %}
        <tr>
            <th>{{ ability.name }} </th>
            <td>{{ ability.total|number_sign }}</td>
            <td>{{ ability.base }}</td>
            <td>
                {{ ability.caracteristicValue|number_sign }}
                <span class="text-muted">({{ ability.caracteristicName }})</span>
            </td>
        </tr>
    {% endfor %}
</table>