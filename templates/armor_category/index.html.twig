{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Armures{% endblock %}

{% block body %}

    <div class="card">
        <div class="card-header">
            <h3>Les catégories d' armures</h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <tr>
                    <th>Nom</th>
                    <th>Nombre d'armures</th>
                </tr>
                {% for category in categories %}
                    <tr>
                        <td>
                            <a href="{{ path('pathfinder_armor_category_show', {'id': category.id}) }}">
                                {{ category.name }}
                            </a>
                        </td>
                        <td>
                            {{ category.armors|length }}
                        </td>
                    </tr>
                {% endfor %}
            </table>
        </div>
    </div>

{% endblock %}
