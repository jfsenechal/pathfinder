{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Pathfinder{% endblock %}

{% block body %}

    {% for level,groupedSpell in groupedSpells %}
        <h5 class="text-success m-3">
            Niveau {{ level }}
        </h5>
        {% for spell in groupedSpell %}
            <div class="card my-2">
                <div class="card-header">
                    <h5>{{ spell.name }}</h5>
                    <span class="text-muted">
                        {% if spell.summary %}
                            {{ spell.summary }}
                        {% else %}
                            {{ spell.description|nl2br }}
                        {% endif %}
                    </span>
                </div>
                <div class="card-body">
                    <a href="{{ path('pathfinder_spell_edit', {'id': spell.id}) }}" class="d-print-none">Edit</a>
                    <div class="row">
                        <div class="col-6">
                            <strong>Durée:</strong>
                            {{ spell.duration }}
                            <br/>
                            <strong> Cible:</strong>
                            {{ spell.target }}
                        </div>
                        <div class="col-6">
                            <strong>Portée:</strong>
                            {{ spell.ranged }}
                            <br/>
                            <strong>Jet de sauvegarde:</strong>
                            {{ spell.savingThrow|string_clean }}
                        </div>
                    </div>
                </div>
            </div>
        {% endfor %}
    {% endfor %}

{% endblock %}
