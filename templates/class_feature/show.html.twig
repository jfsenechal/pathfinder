{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Spell{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3>{{ classFeature }}</h3>
        </div>
        <div class="card-body">
            <table class="table table-bordered">
                <tr>
                    <th>Classe</th>
                    <td>
                        <a href="{{ path('pathfinder_class_show', {'id': classFeature.classT.id}) }}">
                            {{ classFeature.classT }}
                        </a>
                    </td>
                </tr>
                <tr>
                    <th>Référence</th>
                    <td><a href="{{ classFeature.reference }}" target="_pathfinder">{{ classFeature.reference }}</a>
                    </td>
                </tr>
                <tr>
                    <th>Source</th>
                    <td>{{ classFeature.source }}</td>
                </tr>
                <tr>
                    <th>Auto</th>
                    <td>{{ classFeature.auto ? 'Oui' : 'Non' }}</td>
                </tr>
            </table>
            {{ classFeature.description | nl2br }}
        </div>
    </div>
{% endblock %}
