{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Personnage{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3>{{ character }}</h3>
        </div>
        <div class="card-body">
            {{ include('@AfmLibrePathfinder/character/_form.html.twig', {'button_label': 'Update'}) }}

        </div>
    </div>
{% endblock %}
