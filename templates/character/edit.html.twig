{% extends '@AfmLibrePathfinder/base.html.twig' %}

{% block title %}Personnage{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header">
            <h3>{{ character }}</h3>
        </div>
        <div class="card-body">
            {{ include('@AfmLibrePathfinder/character/_form.html.twig') }}
        </div>
        <div class="card-footer">
            <a href="{{ path('pathfinder_character_show', {'uuid': character.uuid}) }}">Annuler</a>
        </div>
    </div>
{% endblock %}
