<a href="{{ path('pathfinder_character_edit', { 'id': character.id }) }}" role="button"
   class="btn btn-primary"><i
            class="far fa-edit"></i> Editer</a>

<a href="{{ path('pathfinder_spell_available_index', { 'id': character.id }) }}" role="button"
   class="btn btn-primary"><i
            class="fas fa-lock"></i> Sorts disponibles</a>

<a href="{{ path('pathfinder_character_index') }}" class="btn btn-primary" role='button'><i class="fas fa-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
    <i class="fas fa-trash"></i> Supprimer
</button>

<br /><br />
