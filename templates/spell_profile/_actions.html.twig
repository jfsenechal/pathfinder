<a href="{{ path('pathfinder_spell_profile_spells_edit', { 'uuid': spellProfile.uuid }) }}" role="button"
   class="btn btn-primary"><i
            class="far fa-edit"></i> Sorts</a>

<a href="{{ path('pathfinder_spell_profile_print', { 'uuid': spellProfile.uuid }) }}" role="button"
   class="btn btn-primary"><i
            class="fas fa-print"></i> Imprimer</a>

<a href="{{ path('pathfinder_spell_profile_edit', { 'uuid': spellProfile.uuid }) }}" role="button"
   class="btn btn-primary"><i
            class="far fa-edit"></i> Renommer</a>

<a href="{{ path('pathfinder_spell_profile_index', {'uuid': character.uuid}) }}" class="btn btn-primary"
   role='button'><i
            class="fas fa-list"></i>
    Retour à la Liste</a>

<button type="button" class="btn btn-danger" data-toggle="modal" data-target="#deleteModal">
    <i class="fas fa-trash"></i> Supprimer
</button>

<br/><br/>
